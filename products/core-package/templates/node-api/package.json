{
  "name": "node-api-template",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Oleg & Neuro Starter Kit v0.4 — Node/API template (Express + Docker + DX).",
  "scripts": {
    "setup": "npm install",
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down -v"
  },
  "lint-staged": {
    "**/*.{js,json}": [
      "prettier --write",
      "eslint --fix"
    ]
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.2.5"
  }
}
